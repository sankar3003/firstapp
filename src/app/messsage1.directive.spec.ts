import { Messsage1Directive } from './messsage1.directive';

describe('Messsage1Directive', () => {
  it('should create an instance', () => {
    const directive = new Messsage1Directive();
    expect(directive).toBeTruthy();
  });
});
